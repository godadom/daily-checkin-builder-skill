# Cookie setup scaffold (not site evidence)

- Acquisition mode: `template`
- Site and scope: reserved scaffold only
- Evidence source: none; a generated project must replace this record from its own sanitized Network or official login evidence

## Exact operator steps

No reusable selector, login URL, F12 request, Console expression, or Cookie name is valid for an unknown site. A generated project must select one evidenced mode: `password_login`, `otp_login`, `interactive_login`, `network`, `console`, or `not_applicable`. Password/OTP modes must link to the generated local headed-browser command. For QingLong, document the exact local helper command, verified OpenAPI settings, destination environment-variable name, and manual panel fallback; do not require a browser inside QingLong.

## Output and transformation

Record the exact sanitized output shape, required Cookie-name whitelist, `HttpOnly` constraints, encoding/transformation rules, and validation request for the target site. Never place a real value here.

## Secret destination

Record the exact protected environment variable or Secret name for the selected deployment platform. Do not claim that a child process can permanently modify its parent shell.

## Expiration and renewal

Record the target site's observed expiration signal, renewal command or exact manual replacement steps, and revocation procedure.
